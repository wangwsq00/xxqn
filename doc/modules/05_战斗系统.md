# 战斗系统设计文档

## 模块概述

本模块定义游戏中的战斗机制，包括副本挑战、PVP对战、自动战斗等。

## 核心内容

### 战斗模式

- 单人战斗
- 多人副本
- PVP竞技
- 公会战

### 战斗机制

#### 阵容设置

- **上阵单位**：攻守双方最多各5个单位
- **位置布局**：从上到下位置号为 4 - 2 - 1 - 3 - 5，1号位在中间

```
位置布局示意图：
    4号位
    2号位
    1号位（中间）
    3号位
    5号位
```

#### 行动条回合制

所有存活的参战单位在同一个行动条上移动：
1. 单位的速度属性决定行动条移动速度
2. 先到达终点的单位发起行动
3. 行动完成后，重置自己在行动条上的位置
4. 循环往复，直到一方全部阵亡

#### 目标选择机制

**主目标优先级**：
1. 首先选择对位单位（1号对1号，2号对2号...）
2. 如果对位单位不可攻击（阵亡、隐身等），按1-5号顺序寻找下一个存活的敌人作为主目标

**目标选择流程**：
```
主目标 = 对位单位
如果 主目标不可攻击：
    遍历1-5号位，找到第一个存活的单位作为主目标
```

#### 攻击方式

根据功法设计，攻击方式分为三种：

| 攻击类型 | 描述 | 适用场景 |
|----------|------|----------|
| 单体攻击 | 只攻击主目标 | 普通攻击、单体技能 |
| 群体攻击 | 攻击所有敌人 | 范围伤害技能 |
| 随机3人 | 攻击主目标 + 另外随机2人 | 多目标技能 |

**普通攻击**：仅单体攻击

### 伤害计算

#### 普通攻击 vs 功法攻击

| 攻击类型 | 功法系数 | 伤害段数 |
|----------|----------|----------|
| 普通攻击 | 100% | 1段 |
| 功法攻击 | 功法定义 | 功法定义 |

#### 伤害计算流程

伤害计算按照以下顺序进行判定：

```
1. 目标选择
2. 命中判定
3. 暴击判定
4. 格挡判定
5. 最终伤害计算
```

#### 1. 命中判定

```
实际命中率 = max(30, 攻击方命中率 - 防御方闪避率)
命中判定 = random(0-100) < 实际命中率

如果未命中：不造成伤害，结束
```

#### 2. 暴击判定

```
实际暴击率 = 攻击方暴击率 - 防御方抗暴击率
实际暴击率 = clamp(实际暴击率, 0, 100)

暴击判定 = random(0-100) < 实际暴击率
暴击伤害系数 = 1.5（暴击时）
```

#### 3. 格挡判定

```
实际格挡率 = 防御方格挡率 - 攻击方抗格挡率
实际格挡率 = clamp(实际格挡率, 0, 100)

格挡判定 = random(0-100) < 实际格挡率
格挡减伤系数 = 0.5（格挡时）
```

#### 4. 基础伤害计算

```
基础伤害 = max(攻击力 - 防御力, 1)
```

#### 5. 完整伤害公式（普通攻击）

```
// 输入参数
攻击方：攻击力、命中率、暴击率、抗格挡率
防御方：防御力、闪避率、抗暴击率、格挡率

// 计算过程
1. 实际命中率 = clamp(命中率 - 闪避率, 30, 100)
2. 实际暴击率 = clamp(暴击率 - 抗暴击率, 0, 100)
3. 实际格挡率 = clamp(格挡率 - 抗格挡率, 0, 100)

4. 如果 random(100) >= 实际命中率：
     伤害 = 0（未命中）

5. 如果 random(100) < 实际暴击率：
     暴击系数 = 1.5
   否则：
     暴击系数 = 1.0

6. 如果 random(100) < 实际格挡率：
     格挡系数 = 0.5
   否则：
     格挡系数 = 1.0

7. 最终伤害 = max(攻击力 - 防御力, 1) × 暴击系数 × 格挡系数
```

#### 6. 功法攻击伤害计算

功法攻击在基础伤害上乘以功法伤害系数，并且支持多段伤害。

**单段伤害功法**：
```
最终伤害 = 基础伤害 × 功法伤害系数 × 暴击系数 × 格挡系数
```

**多段伤害功法**：
每段伤害单独计算命中、暴击、格挡，并且可能有触发中断机制。

```
总伤害 = 0
触发了前一段 = true

for (段数 from 1 to 功法总段数) {
    if (触发了前一段) {
        // 检查本段是否触发中断
        if (本段有触发概率要求) {
            if (random(100) >= 触发概率) {
                break  // 中断，后面不再触发
            }
        }
        
        // 计算本段伤害
        实际命中率 = clamp(命中率 - 闪避率, 30, 100)
        
        if (random(100) < 实际命中率) {
            暴击系数 = (random(100) < 实际暴击率) ? 1.5 : 1.0
            格挡系数 = (random(100) < 实际格挡率) ? 0.5 : 1.0
            
            本段伤害 = max(攻击力 - 防御力, 1) × 功法本段伤害系数 × 暴击系数 × 格挡系数
            总伤害 += 本段伤害
        }
        
        触发了前一段 = true
    }
}

return 总伤害
```

#### 伤害计算示例 - 普通攻击

**场景**：攻击方 vs 防御方

| 属性 | 攻击方 | 防御方 |
|------|--------|--------|
| 攻击力 | 10,000 | - |
| 防御力 | - | 8,000 |
| 命中率 | 80 | - |
| 闪避率 | - | 30 |
| 暴击率 | 20 | - |
| 抗暴击率 | - | 10 |
| 格挡率 | - | 15 |
| 抗格挡率 | 5 | - |

**计算过程**：
1. 实际命中率 = max(30, 80 - 30) = 50
2. 实际暴击率 = 20 - 10 = 10
3. 实际格挡率 = 15 - 5 = 10
4. 基础伤害 = 10,000 - 8,000 = 2,000

**假设情况**：
- 命中 + 暴击 + 格挡：2,000 × 1.5 × 0.5 = 1,500
- 命中 + 暴击 + 未格挡：2,000 × 1.5 = 3,000
- 命中 + 未暴击 + 格挡：2,000 × 0.5 = 1,000
- 命中 + 未暴击 + 未格挡：2,000
- 未命中：0

#### 伤害计算示例 - 七星剑阵

**场景**：攻击方使用七星剑阵 vs 防御方（同上方属性）

**七星剑阵伤害段数**：

| 段数 | 伤害系数 | 触发概率 | 说明 |
|------|----------|----------|------|
| 第1剑 | 30% | 100% | 必定触发 |
| 第2剑 | 40% | 100% | 必定触发 |
| 第3剑 | 50% | 70% | 中断则后面不触发 |
| 第4剑 | 60% | 70% | 中断则后面不触发 |
| 第5剑 | 70% | 70% | 中断则后面不触发 |
| 第6剑 | 80% | 70% | 中断则后面不触发 |
| 第7剑 | 100% | 70% | 中断则后面不触发 |

**七星剑阵计算过程**：

```
总伤害 = 0
触发了前一段 = true

// 第1剑（30%，100%触发）
if (random(100) < 50) {
    暴击系数 = (random(100) < 10) ? 1.5 : 1.0
    格挡系数 = (random(100) < 10) ? 0.5 : 1.0
    本段伤害 = 2000 × 0.3 × 暴击系数 × 格挡系数
    总伤害 += 本段伤害
}
触发了前一段 = true

// 第2剑（40%，100%触发）
if (random(100) < 50) {
    暴击系数 = (random(100) < 10) ? 1.5 : 1.0
    格挡系数 = (random(100) < 10) ? 0.5 : 1.0
    本段伤害 = 2000 × 0.4 × 暴击系数 × 格挡系数
    总伤害 += 本段伤害
}
触发了前一段 = true

// 第3-7剑（50%-100%，70%触发，可能中断）
for (段数 from 3 to 7) {
    if (random(100) >= 70) {
        break  // 中断
    }
    
    if (random(100) < 50) {
        暴击系数 = (random(100) < 10) ? 1.5 : 1.0
        格挡系数 = (random(100) < 10) ? 0.5 : 1.0
        功法系数 = [0.5, 0.6, 0.7, 0.8, 1.0][段数-3]
        本段伤害 = 2000 × 功法系数 × 暴击系数 × 格挡系数
        总伤害 += 本段伤害
    }
}

return 总伤害
```

**七星剑阵期望伤害示例**（假设所有命中、无暴击无格挡）：
- 理想情况（7剑全中）：2000×(0.3+0.4+0.5+0.6+0.7+0.8+1.0) = 2000×4.3 = **8,600**
- 典型情况（出5剑）：2000×(0.3+0.4+0.5+0.6+0.7) = 2000×2.5 = **5,000**

### 战斗奖励

待补充...

## 与其他模块的关联

- 角色系统：角色属性决定战斗力
- 功法系统：战斗中使用功法，决定攻击方式和伤害
- 灵宠系统：灵宠参与战斗
- 经济系统：战斗产出资源
