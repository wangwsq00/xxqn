# M2 表现层

只改怎么看，不改战斗公式、伤害、存档键、关卡奖励和 Pages 部署。可以新增展示用资源键，不新增属性。

## 法阵阶

洞府地面铺聚灵阵精灵，主角立绘 `origin(0.5, 1)`，脚底仍是 `(360, 780)`。现有立绘是方图半身，所以用软晕边去掉金框，坐在法阵中心。青粒子从阵面升到胸口，身体在 `1.00` 与 `1.02` 之间呼吸，胸口有一层径向光。

阶越高，转得越快、粒子越密、光越亮。

| 条件 | 阶 | 文件 | Phaser 键 |
|------|----|------|-----------|
| 聚灵阵 1–3 级 | 1 | `public/assets/fx/array_tier1.png` | `array_tier1` |
| 聚灵阵 4–6 级 | 2 | `public/assets/fx/array_tier2.png` | `array_tier2` |
| 聚灵阵 7–10 级 | 3 | `public/assets/fx/array_tier3.png` | `array_tier3` |

等级为 0、缺失或不是数字时，改按大境：炼气–金丹一阶，元婴–炼虚二阶，合体–渡劫三阶。

附件原图是 1280×720 的横幅底衬。入库前裁成圆形精灵并去掉平底，才能铺在洞府地面上。

## 底栏图标

洞府底栏仍是 **洞府 / 试炼 / 养成**，跳转不变。图标在短标签上面。热区沿用 `DOCK_HIT_HEIGHT`（120，不小于 88）。选中是朱砂底加赤金描边，不是纯文字砖。

| 标签 | 文件 | Phaser 键 |
|------|------|-----------|
| 洞府 | `public/assets/ui/icon_dongfu.png` | `icon_dongfu` |
| 试炼 | `public/assets/ui/icon_trial.png` | `icon_trial` |
| 养成 | `public/assets/ui/icon_growth.png` | `icon_growth` |

左上角圆形头像是主角脸部裁切。旁边是境界名和灵石。点头像打开「详细属性」，列出当前主角已有的生命、攻击、防御、速度、命中、闪避、暴击、抗暴击、格挡、抗格挡。数值走开战用的 `makeHero`（装备和功法加成），不另造战力。点关闭、点遮罩或再点头像可退回。顶上的「修仙千年」标题已去掉，避免和头像卡抢位置。修为条和领取按钮逻辑不变。

## 战斗站位

单位用软晕边立绘站在战场上，没有立绘牌。血条和短名字浮在头顶。空位只留一枚淡金色地标。槽位脚底、目标选择和功法逻辑不变。

- 普攻：向目标冲刺约 150ms，再回到原位，命中时闪白并画一道斩击。
- 功法：短暂放大、变色，并飞出一枚光点。护盾类没有目标，只做施法姿态。
- 阵亡：朝脚底塌缩并淡出。

## 共用速度条

战场标题下方一条横条。每个存活单位一枚圆形头像，我方青边、敌方朱砂边。

位置只读战斗单位当前的 `atb / ATB_MAX`（引擎满值 1000，每 tick 加该单位速度）。不另做一条计时。速度快的填得快，可以在慢单位出手前行动两次，图标和引擎一致。

出手那一 tick 引擎会把该单位 `atb` 清零。动画期间把头像钉在终点并加金边，表示「已经走到头、正在行动」；动画结束再按真实的 0 跳回起点，避免倒着滑过整条。单位阵亡后头像淡出。每人头顶不再画单独的行动条。
